"""Evaluation, ablations, falsification suite (P13).

Agent code MUST NOT import this package; that boundary is enforced
mechanically by tests/tripwires/test_metrics_isolation.py. Profit, if it
appears, is a measured outcome computed here and only here — never an
input to any decision. Harness-only channels (ground-truth queue position,
engine-side account state) terminate in this package (INV-11).
"""
