"""Experiment configurations and the ablation runner (P13, measurement side).

This package is part of the measurement instrument, not the agent: like
tests/, it may import ``topos.metrics`` (the repo tripwire allowlists
exactly tests/ and experiments/). No agent-facing code imports anything
from here.
"""
